{
  "name": "sistema-cobranza-twilio",
  "version": "1.0.0",
  "description": "Sistema de gestión de campañas de cobranza con Twilio - SMS y llamadas automáticas",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "node server.js",
    "check-env": "node -e \"require('dotenv').config(); console.log('✅ Variables de entorno cargadas:', Object.keys(process.env).filter(k => k.startsWith('TWILIO')))\"",
    "setup": "npm install && echo '\\n✅ Dependencias instaladas\\n⚠️  Crea tu archivo .env basándote en .env.example\\n'"
  },
  "keywords": [
    "twilio",
    "sms",
    "cobranza",
    "llamadas",
    "automatizacion"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "twilio": "^5.10.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/TU_USUARIO/sistema-cobranza-twilio.git"
  }
}